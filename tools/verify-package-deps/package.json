{
  "name": "verify-package-deps",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "packageManager": "npm@10.9.2",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "verify": "node --experimental-strip-types src/verify-package-deps.ts",
    "verify:fix": "node --experimental-strip-types src/verify-package-deps.ts --fix",
    "check-types": "tsc",
    "format": "prettier --write .",
    "check-format": "prettier --check .",
    "lint": "oxlint --quiet --config ../../oxlint.config.ts"
  },
  "dependencies": {
    "oxc-parser": "^0.94.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.5",
    "typescript": "~5.9.3"
  }
}
