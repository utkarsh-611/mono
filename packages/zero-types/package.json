{
  "name": "zero-types",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "check-types": "tsc",
    "check-types:watch": "tsc --watch",
    "format": "oxfmt .",
    "check-format": "oxfmt --check .",
    "lint": "oxlint --type-aware src/",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "oxfmt": "^0.44.0",
    "shared": "0.0.0",
    "typescript": "~6.0.2",
    "vitest": "4.1.2"
  }
}
