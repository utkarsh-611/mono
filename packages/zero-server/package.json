{
  "name": "zero-server",
  "version": "0.0.0",
  "private": true,
  "description": "Library to include in API servers for processing mutation and query requests",
  "type": "module",
  "scripts": {
    "format": "oxfmt .",
    "check-format": "oxfmt --check .",
    "check-types": "tsc",
    "check-types:watch": "tsc --watch",
    "lint": "oxlint --type-aware src/",
    "postinstall": "prisma generate --schema ./src/adapters/schema.test.prisma",
    "test": "vitest run",
    "test:watch": "vitest watch",
    "test:pg17": "vitest run --project='*17*'",
    "test:pg17:watch": "vitest watch --project='*17*'",
    "fmt": "oxfmt .",
    "check-fmt": "oxfmt --check ."
  },
  "dependencies": {
    "@rocicorp/logger": "^5.4.0"
  },
  "devDependencies": {
    "@prisma/adapter-pg": "^7.2.0",
    "@prisma/client": "^7.2.0",
    "@vitest/runner": "4.1.3",
    "drizzle-orm": "^0.45.1",
    "oxfmt": "^0.45.0",
    "pg": "^8.16.3",
    "postgres": "3.4.7",
    "prisma": "^7.2.0",
    "shared": "0.0.0",
    "vitest": "4.1.3",
    "z2s": "0.0.0",
    "zero-cache": "0.0.0",
    "zero-protocol": "0.0.0",
    "zero-schema": "0.0.0",
    "zero-types": "0.0.0",
    "zql": "0.0.0"
  }
}
